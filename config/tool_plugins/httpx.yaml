name: httpx
tier: low
timeout_sec: 300
command_template: httpx -u {target} -silent -status-code -title -tech-detect -follow-redirects -x all
description: Fast and multi-purpose HTTP toolkit for probing.
risk: low
